# src/services

Integration and API client code lives here. Keep a clear separation between client UI and server-side responsibilities.

Examples:
- `payments/` (Razorpay client wrappers), `apiClient.js`.
